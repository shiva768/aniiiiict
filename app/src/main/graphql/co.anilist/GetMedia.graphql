query GetMedia($id: Int!) {
  Media(id: $id) {
    id
    format
    episodes
    status
    nextAiringEpisode {
      episode
      airingAt
    }
    genres
    studios(isMain: true) {
      nodes {
        name
      }
    }
  }
}