mutation UpdateStatus($workId: ID!, $state: StatusState!) {
  updateStatus(input: {
    workId: $workId,
    state: $state,
    clientMutationId: "aniiiiiict-update-status"
  }) {
    work {
      id
      viewerStatusState
    }
    clientMutationId
  }
} 