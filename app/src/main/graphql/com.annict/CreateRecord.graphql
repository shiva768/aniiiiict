mutation CreateRecord($episodeId: ID!) {
  createRecord(input: {
    episodeId: $episodeId,
    clientMutationId: "aniiiiiict-create-record"
  }) {
    clientMutationId
    record {
      id
    }
  }
} 