mutation CreateRecord($episodeId: ID!) {
  createRecord(input: {
    episodeId: $episodeId,
    clientMutationId: "aniiiiict-create-record"
  }) {
    clientMutationId
    record {
      id
    }
  }
} 