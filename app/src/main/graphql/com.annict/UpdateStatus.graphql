mutation UpdateStatus($workId: ID!, $state: StatusState!) {
  updateStatus(input: {
    workId: $workId,
    state: $state,
    clientMutationId: "aniiiiict-update-status"
  }) {
    clientMutationId
    work {
      id
    }
  }
} 